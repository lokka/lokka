%h2= t('edit_comment')
= partial 'comments/form', :views => Lokka.admin_theme_dir, :layout => :admin_layout, :locals => {:action => url("/admin/comments/#{@comment.id}"), :method => 'put', :label => t('save')}
