%h2= t('new_field_name')
= partial 'field_names/form', :views => Lokka.admin_theme_dir, :layout => :admin_layout, :locals => {:action => url('/admin/field_names'), :method => 'post', :label => t('new')}
