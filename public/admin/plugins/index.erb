<h2><%= t('listing_plugins') %></h2>
<ul class="plugins">
  <% settings.plugins.each do |plugin| %>
    <li><%= link_to plugin.name.titleize, url("/admin/plugins/#{plugin.name}"), if: plugin.have_admin_page %></li>
  <% end %>
</ul>
