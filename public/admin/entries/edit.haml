%h2= t("edit_#{@name}")
= partial 'entries/form', :views => Lokka.admin_theme_dir, :layout => :admin_layout, :locals => {:action => url("/admin/#{@name.pluralize}/#{@entry.id}"), :method => 'put', :label => t('save')}
