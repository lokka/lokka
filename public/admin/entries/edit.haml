%h2= rt["edit_#@name"]
%form(action="/admin/#{@name.pluralize}/#{@entry.id}" method="post")
  %input(type="hidden" name="_method" value="put")
  = partial 'entries/form'
  %p
    %input(type="submit" value="#{rt.preview}" name="preview")
    %input(type="submit" value="#{rt.edit}" name="edit")
